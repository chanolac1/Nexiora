# NCOS-022 Repair1 — Apply Contract Unification

Repara la duplicación del contrato público de `apply` entre `NxPackageManager.h` y `NxPackageApply.h`.

Instalar mediante el Package Manager existente y recompilar `nexiora_package` y `NxPackageManagerTests`.
