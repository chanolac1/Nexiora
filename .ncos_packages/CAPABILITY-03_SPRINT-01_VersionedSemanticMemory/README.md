# CAPABILITY-03 Sprint-01 — Versioned Semantic Memory

Paquete Pure Apply. Descomprima sobre `D:\Nexiora` y ejecute:

```powershell
.\Build\windows-msvc-release\bin\nexiora_package.exe
```

Artefacto principal:

```text
Build\windows-msvc-release\bin\nexiora_semantic_memory.exe
```
