# CAPABILITY-02 Sprint-05 — Unified Cognitive Dialogue

Paquete Pure Apply. Descomprima sobre `D:\Nexiora` y ejecute:

```powershell
.\Build\windows-msvc-release\bin\nexiora_package.exe
```

Artefacto principal:

```text
Build\windows-msvc-release\bin\nexiora_cognitive_dialogue.exe
```
